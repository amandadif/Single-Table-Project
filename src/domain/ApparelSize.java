package domain;

/**
 * use .ordinal() to convert the enum into an integer when saving it to SQLite
 * Small = 0, Medium = 1, Large = 2, Extra Large = 3
 */
public enum ApparelSize {
  SMALL,
  MEDIUM,
  LARGE,
  EXTRA_LARGE
}
